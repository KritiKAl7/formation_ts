import React, { useState } from "react";
import { Select } from "antd";

function CaptainChooser(props) {
  const [selectedOption, setSelectedOption] = useState({});
}

export default CaptainChooser;
